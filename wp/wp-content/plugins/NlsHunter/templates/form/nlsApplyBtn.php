<?php

/**
 * @text
 * @class
 * @prepend
 * @append
 * @iconSize
 */
?>

<button type="submit" class="<?= isset($class) ? $class : '' ?>"><?= isset($text) ? $text : __('Submit', 'NlsHunter') ?></button>